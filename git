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

update user.name & user.email:
git config --global user.name "A name"
git config --global user.name an-email@gmail.com
view above: git config --list

don't work
{
clone repo with username and password:
git clone https://mygitusername:mygitpassword@github.com/somedir/pathtorepo.git

already clone, to set username, password:
git remote set-url origin https://mygitusername:mygitpassword@github.com/somdir/pathtorepo.git
}

git config credential.helper store            : store credential in local repo
git config --global credential.helper store   : store credential in local globally
-->type username&pw for the first time and use forever 
-->insecure
More secure
git config --global credential.helper 'cache --timeout=3600'

setup:
git init

add origin
git remote add origin https://github.com/somedir/repo.git

see local branches:
git branch
see remote branches:
git branch -r


create new local branch:
git checkout -b branchname

delete local branch:
git branch -d branchname

push to remote
git push origin master
git push origin branchname
git push <remote-name> branchname
git push <remote-name> <local-branch-name>:<remote-branch-name>
git push origin branch1:branch1

overwrite local branch to master
git push --force origin branch1

pull:
git pull
git pull branch

For unrelated history:

fetch:
git fetch

Merge in local:
git merge origin/remote-branch			(-->to master)
git merge origin/remote-branch local-branch
First time (fatal: refusing to merge unrelated histories):
git merge origin/remote-branch local-branch --allow-unrelated-histories

push merged to remote:
git push origin branchlocalt

clone project:
git clone remoterepository localfolder



